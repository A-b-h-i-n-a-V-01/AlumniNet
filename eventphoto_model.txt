
> class EventPhoto(db.Model):
      id = db.Column(db.Integer, primary_key=True)
      user_id = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)
      image_path = db.Column(db.String(200), nullable=False)
      caption = db.Column(db.String(200))
      event_name = db.Column(db.String(100))
      category = db.Column(db.String(50), default='event') # 'event' or 
'job_poster'
      status = db.Column(db.String(20), default='pending') # pending, 
approved, rejected
      verified_by = db.Column(db.Integer, db.ForeignKey('user.id'))
      uploaded_at = db.Column(db.DateTime, nullable=False, 
default=datetime.utcnow)
  
      # Relationships
      uploader = db.relationship('User', foreign_keys=[user_id], 
backref=db.backref('uploaded_photos', cascade="all, delete-orphan"))
      verifier = db.relationship('User', foreign_keys=[verified_by], 
backref='verified_photos')
  


